<%= form_for(@comment_event) do |f| %>
  <% if @comment_event.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@comment_event.errors.count, "error") %> prohibited this comment_event from being saved:</h2>

      <ul>
      <% @comment_event.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br />
    <%= f.number_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :content %><br />
    <%= f.text_area :content %>
  </div>
  <div class="field">
    <%= f.label :event_id %><br />
    <%= f.number_field :event_id %>
  </div>
  <div class="field">
    <%= f.label :score_id %><br />
    <%= f.number_field :score_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
