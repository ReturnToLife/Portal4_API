<%= form_for(@score) do |f| %>
  <% if @score.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@score.errors.count, "error") %> prohibited this score from being saved:</h2>

      <ul>
      <% @score.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :score_pos %><br />
    <%= f.number_field :score_pos %>
  </div>
  <div class="field">
    <%= f.label :score_neg %><br />
    <%= f.number_field :score_neg %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
