<%= form_for(@gcomment) do |f| %>
  <% if @gcomment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@gcomment.errors.count, "error") %> prohibited this gcomment from being saved:</h2>

      <ul>
      <% @gcomment.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br />
    <%= f.number_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :body %><br />
    <%= f.text_area :body %>
  </div>
  <div class="field">
    <%= f.label :gossip_id %><br />
    <%= f.number_field :gossip_id %>
  </div>
  <div class="field">
    <%= f.label :score_id %><br />
    <%= f.number_field :score_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
